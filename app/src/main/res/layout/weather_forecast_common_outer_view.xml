<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/content_weather_forecast_main_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:layout_below="@+id/lbl_type_of_forecast">

    <ImageView
        android:id="@+id/img_weather_type_full"
        android:layout_width="@dimen/full_img_dimension"
        android:layout_height="@dimen/img_dimension"
        android:paddingTop="@dimen/padding_between_views"
        android:layout_centerHorizontal="true"
         />

    <TextView
        android:id="@+id/tv_summary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/img_weather_type_full"
        android:paddingTop="@dimen/padding_between_views"/>
</RelativeLayout>
